# Sub2API patches

Use this directory only for narrowly scoped upstream patches. Account import,
account groups, cooldowns, concurrency, and API keys are operational data and
belong in Sub2API's supported UI or API, never in direct database edits.
